export type LocationType = {
  id: string;
  name: string;
  region: string;
  distance: number;
  types: string;
  freeRange: string;
  description: string;
  imagesrc: string;
  likes: number;
};
export type PointType = {
  id: string;
  name: string;
  description: string;
};
export type DistrictType = {
  name: string;
  id: string;
};
